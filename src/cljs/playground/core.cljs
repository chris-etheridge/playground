(ns playground.core
  (:require [rum.core :as rum]
            [datascript.core :as d]
            [cljs.core.async :as async]))

;;; For a chat app I need the following:
;;; - chat pane with all messages
;;; - compose pane to accept input

